You are an expert Python developer specialized in python-telegram-bot (version 20+) and MongoDB (`pymongo`). Implement a comprehensive, fully interactive "Entertainment and Gaming Engine" powered entirely by solid Telegram Bot API 9.4 colored inline keyboards.

CRITICAL UI & ARCHITECTURAL RULES:
1. STRICT NO EMOJI POLICY EXCEPT FOR SPECIFIC GAME PIECES: Absolutely no decorative emojis anywhere in the menus, lists, or stats. Use premium Arabic typographic symbols (❖, ✦, ◈, ▲, ▼, ➔, ◀). EXCEPTIONS: The letters 'X' and 'O' (or standard bold X/O text characters) are strictly allowed inside the XO game grid layout only.
2. BUTTON STYLING (API 9.4): Every single keyboard layout generated by this module must enforce solid colored backgrounds using the `style` property ('success' for green, 'primary' for blue, 'danger' for red). No transparent/gray native buttons.
3. DATABASE INTEGRATION: Connect all games to MongoDB to update a universal user `points` field, along with a nested profile object tracking `game_stats: {xo_wins: 0, xo_losses: 0, total_games: 0}`.
4. MAIN TOGGLE COMMANDS:
   - Implement `تعطيل الالعاب` (Disables all game system execution in the group. Restrict activation to Developer, Group Owner, and Creator ranks only).
   - Implement `تفعيل الالعاب` (Enables game processing. Updates configuration instantly in MongoDB).
   - Middleware check: If games are disabled, completely ignore any game trigger words.

MODULE 1: THE INTERACTIVE MCQ QUIZ ENGINE (اختر الإجابة الصحيحة / صح أم خطأ)
- Triggers: Text commands `سؤال` or `صح ام خطا`.
- Logic: Pull a random question from a localized Python dictionary bank containing diverse general knowledge questions.
- Layout: Render the question in HTML formatting. Generate 4 multi-choice answer buttons or 2 True/False buttons using the 'primary' (Blue) style.
- Handling: When a user clicks an option, check its validity via callback data. If correct, update their MongoDB records with preset points, edit the message to announce their victory dynamically, and convert all button styles to 'success' (Green) for the right answer and 'danger' (Red) for wrong answers while locking further interaction.

MODULE 2: ULTIMATE XO GAME ENGINE (لعبة إكس أو)
- Invocation Types:
  1. Reply Mode: User types `xo` in reply to another member. This initiates a direct challenge locked strictly to that target user.
  2. Public Open Mode: User types `xo` without a reply. The bot sends an open invitation text: "<b>❖ بانتظار منافس للانضمام إلى مباراة XO...</b>", attaching a solid 'success' (Green) button labeled "قبول المباراة". Anyone other than the host can click to join.
- Game Interface & Play:
  - Once accepted, render a clean 3x3 interactive matrix grid using solid inline buttons. Empty fields should display a typographic placeholder character (e.g., ◈) styled as 'primary'.
  - Game pieces must toggle strictly between 'X' and 'O' representations upon valid turns.
  - Dynamically update the board layout via `edit_message_reply_markup` on every turn, updating the header text to show whose turn it currently is.
- Victory Conditions: Validate winning rows/columns/diagonals after each turn. If a player wins, award points to the winner, increment their MongoDB stats, and locked the board with a definitive HTML announcement. If a tie occurs, declare a draw with zero points distributed.
- Statistics Commands:
  * `احصائياتي`: Displays the calling user's game wins, losses, and total points in HTML format.
  * `ترتيب اللاعبين`: Queries MongoDB to fetch the top 10 players sorted by points/wins and displays a premium typographic leaderboard rank list.

MODULE 3: INTEGRATION OF THE 4 NEW SPECIAL CONTEXT GAMES
Implement highly optimized handlers for these four custom gaming ecosystems:
1. Speed Math Challenge (`تحدي الرياضيات`): Generates random mathematical equations. Displays 4 choice buttons. First correct click gets immediate points.
2. Rock Paper Scissors (`حجرة ورقة مقص`): Reply-based or open invitation setup. Tracks dual hidden selections using interactive callback arrays before calculating the final match result on-screen.
3. Minefield / Treasure Hunt (`حقل الالغام`): Generates a dynamic 4x4 interactive grid matrix of solid buttons. 15 buttons contain random scaling points ('success' styling), while 1 button triggers a hidden bomb ('danger' styling). Users take turns clicking fields; clicking points harvests them instantly, hitting the bomb terminates the matrix immediately and deducts points.
4. Smart Number Guessing (`تخمين الرقم`): Single-player instance. The bot selects a secret number between 1 and 50. Provides control adjustment buttons (+1, -1, +5, -5) and a "تأكيد التخمين" action trigger. Guides the player with interactive textual "أعلى ▲" or "أقل ▼" hints within a maximum lifecycle of 5 sequential attempts.

Ensure all callback routines are completely secure against unauthorized user click cheating, and write fully asynchronous, robust python-telegram-bot code handlers.
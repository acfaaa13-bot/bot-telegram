You are an expert Python developer. Apply the following strict architectural fixes, logic updates, and database integrations to the Telegram Group Management Bot using `python-telegram-bot` (v20+) and MongoDB (`pymongo`).

CRITICAL UI & PERSONALITY COMPLIANCE:
- NO EMOJIS in any text responses, keyboards, logs, or menus. Use elegant typographic symbols (❖, ✦, ◈, ▲, ▼, ➔, ◀, 【, 】).
- NO AI CLICHÉS: The bot must NEVER describe itself as an AI or a protection bot. Keep the user interface completely clean, minimalist, and direct.
- BUTTON STYLING (API 9.4): All inline buttons must have solid backgrounds via the `style` property ('success' for green, 'danger' for red, 'primary' for blue).

1. COMMAND VS BUTTON INTERACTION OVERRIDE:
- Disable all text-based filter commands like "قفل الصور", "تعطيل التعديل", etc. The bot must NOT respond to text commands for toggling locks. Group protection settings, media restrictions, and auto-mute status can ONLY be altered via their respective interactive Inline Keyboards (`حماية المجموعة`, `الوسائط`, `الكتم التلقائي`).
- Keep manual configuration text commands active using strict regex:
  * `تعيين عدد التحذيرات ` + [number] (e.g., تعيين عدد التحذيرات 3)
  * `تعيين مده الكتم ` + [number] (e.g., تعيين مده الكتم 10)

2. RANK HIERARCHY & FIXED PERMISSIONS FIX:
- Establish a fixed permissions matrix for each custom rank (Owner, Creator, Admin, Moderator) stored in MongoDB.
- When a user is promoted (`رفع`) or demoted (`تنزيل`), save their explicit role in MongoDB. 
- Ensure that the rank permissions can only be modified by the Group Owner, Creator, or the specific higher rank that promoted that specific user (strict hierarchy check).

3. BOT PRIVATE CHAT (PV) ARCHITECTURE FIX:
- In private chat (`/start`), the bot must display a clean main user menu containing navigation buttons to access: Available commands help text, Source channel link, and Developer profile information.
- Ensure that Global Replies (`الردود العامة`) added via the Developer Panel work universally across both private chats and all groups.

4. DUAL-LAYER MANDATORY SUBSCRIPTION ENGINE FIX:
- Update the subscription parser for the command `اشتراك اجباري ` + [Link, Username, or ID] to correctly extract and resolve Telegram channel URLs, @usernames, or raw numerical IDs.
- Middleware fix: If a regular user sends a message and violates either the Developer subscription, the Group Owner subscription, or BOTH, the bot MUST instantly delete their message. It must reply immediately with an HTML alert block and display the channel join buttons side-by-side in a single row.
- Admin Panel addition: Add a dedicated button styled as 'danger' labeled "تعطيل الاشتراك الإجباري" inside the Owner/Creator management menu to easily deactivate the group-level subscription check.

5. DEVELOPER PANEL & PROFILE DYNAMIC FIX:
- Add exactly two configuration buttons inside the Developer Panel (`/panel`):
  * "تعيين يوزر المطور" (Sets the developer's @username in MongoDB).
  * "تعيين قناة السورس" (Sets the source channel's @username or link in MongoDB).
- Trigger Fix: When any user types `مطور` or `المطور` in a chat, the bot must dynamically fetch these configurations from MongoDB, present the developer profile info cleanly, and attach a solid 'primary' (Blue) inline button linking directly to the configured developer account.

6. PUNISHMENT SYSTEM ACTIONS UPGRADE (ID-Based & Mute Button):
- ID-Based Execution: Modify the commands `كتم`, `حظر`, `فك كتم`, and `فك الحظر` to accept a raw Telegram User ID right after the command keyword (e.g., `كتم 12345678`), allowing staff to punish users without needing to reply to a message.
- Inline Unmute Button: Whenever a user is muted (manually by an admin or automatically via the auto-mute threshold), the bot's HTML confirmation message MUST include a solid 'danger' (Red) inline button at the bottom labeled "فك الكتم" pointing to the specific user's unmute callback handler.

7. INTERACTIVE NAVIGATION FIX:
- Every open submenu or configuration dashboard generated by the bot (e.g., `حماية المجموعة`, `الوسائط`, `الكتم التلقائي`, etc.) MUST feature a dedicated navigation button at the very bottom row styled as 'danger' (Red) labeled "◀ رجوع" to return smoothly to the parent main menu without deleting the message.

Review the entire database routing to ensure all data reads and writes execute asynchronously without blocking, resolving all mentioned bugs perfectly.